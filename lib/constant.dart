import 'package:flutter/material.dart';

class AppConstants {
  static const Color primaryColor = Colors.blue;
  static const Color secondaryColor = Colors.lightBlueAccent;
  static const Color buttonTextColor = Colors.white;
  static const Color buttonBackgroundColor = primaryColor;
  static const Color buttonPressedColor = primaryColor;
  static const double buttonMaxWidth = 150.0;
  static const double buttonHeight = 50.0;
   static const EdgeInsets buttonPadding = EdgeInsets.symmetric(vertical: 12.0, horizontal: 20.0);
  static const BorderRadius buttonBorderRadius = BorderRadius.all(Radius.circular(10));
  static const userName = "Tomflutter";
  static const jobTitle = "Flutter Developer";
  static const Color backgroundColor = Colors.green;
  static const Color jancok = Colors.white30;
}
